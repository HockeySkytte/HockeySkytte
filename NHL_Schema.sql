# Create a new Schema/Database for our tables. I've called the database Public, because I plan to put public data from NHL, PWHL and AHL here.
CREATE SCHEMA `public`;

# Select the newly created Schema.
USE public;